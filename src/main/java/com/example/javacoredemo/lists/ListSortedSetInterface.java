package com.example.javacoredemo.lists;

public class ListSortedSetInterface {

    /**
     *
     * The Java SortedSet interface, java.util.SortedSet, is a subtype of the java.util.Set interface. The Java
     * SortedSet interface behaves like a normal Set with the exception that the elements it contains are sorted
     * internally. This means that when you iterate the elements of a SortedSet the elements are iterated in the
     * sorted order.
     *
     * This interface contains the methods inherited from the Set interface and adds a feature that stores all the
     * elements in this interface to be stored in a sorted manner.
     *
     * SortedSet Interface in Java with Examples
     * methods (18)
     * https://www.geeksforgeeks.org/sortedset-java-examples/
     */
}
